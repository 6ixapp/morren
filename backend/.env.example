# Server Configuration
PORT=5000
NODE_ENV=production

# Database
# Local: install PostgreSQL, create a DB, then use postgresql://user:password@127.0.0.1:5432/your_db
# Railway: add PostgreSQL in project and reference DATABASE_URL from the Postgres service
DATABASE_URL=postgresql://user:password@127.0.0.1:5432/morren_db

# JWT Configuration (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production

# Access Token: Short-lived for security (1-4 hours recommended)
JWT_EXPIRES_IN=4h

# Refresh Token: Long-lived for mobile app persistence (30 days recommended)
# This allows users to stay logged in for up to 30 days on mobile devices
# Mobile apps store tokens in encrypted hardware-backed storage
JWT_REFRESH_EXPIRES_IN=30d

# CORS Configuration (Update with your frontend URL)
# For production, this is auto-configured based on NODE_ENV
# Development will allow localhost, production allows Vercel domains
CORS_ORIGIN=https://your-frontend-app.vercel.app

# OneSignal Push Notifications
# Get these from your OneSignal dashboard: https://onesignal.com
ONESIGNAL_APP_ID=your-onesignal-app-id
ONESIGNAL_REST_API_KEY=your-onesignal-rest-api-key
